
<view class="page">
  <view class="head" wx:if="{{key=='schoolmateId'}}">
   <view class="avatar">
     <image class="img" src="{{data.imgUrl}}"></image>
   </view>
   <view class="name">{{data.name}}</view>
  </view>
  <rich-text nodes="{{data.content}}"></rich-text>
</view>
